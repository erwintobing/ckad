# 21 - Failing Pod Debugging
Cluster: ckad0021<br>
Namespace: dev<br>
Doc links: Pods

## Task

1. The application deployed via the startup-app.yaml manifest fails to properly deploy. Deploy the pod, then troubleshoot the issue by checking the logs, debug and events.

2. Once finished troubleshooting confirm the source of the issue has been resolved by exec the container and verifying the scripts and init folder.
